{
  "name": "back-ping",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/back/ping/src",
  "projectType": "library",
  "tags": ["back"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/back/ping/jest.config.ts"
      }
    }
  }
}
